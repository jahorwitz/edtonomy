import React from "react";
import ReactDOM from "react-dom/client";
import "./default.css";

import NpsFormModal from "../src/components/NpsFormModal/NpsFormModal";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NpsFormModal />
  </React.StrictMode>
);
